@model IEnumerable<AddressBook.Models.Telephone>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { contact = ViewBag.Contact})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Number)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.EditorFor(modelItem => item.Number)
            </td>
            <td>
                @Html.ActionLink("Delete", "Delete", new { id = item.TelephoneId })
            </td>
        </tr>
    }

</table>


